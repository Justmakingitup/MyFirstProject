using System;
using System.Collections.Generic;

namespace practice
{
    class Program
    {
        static void Main(string[] args)
        {
            List<int> numbers = new List<int>();

            


            for(int i = 0; i >=10; i++){
                Console.WriteLine("please enter numbers to be added together");
                
                string userInput = Console.ReadLine();

                if (i > 0 && i <= 10) {
                    
                } 
                
            }
        }
    }
}
